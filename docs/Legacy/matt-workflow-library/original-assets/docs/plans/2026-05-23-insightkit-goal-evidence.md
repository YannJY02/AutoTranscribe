# InsightKit Goal Evidence Ledger - 2026-05-23

This ledger tracks current evidence for the goal of making InsightKit a personal Feishu/Lark Minutes replacement as described in `docs/Legacy/overview.md`.

## Verified This Cycle

- Goal-level machine-readable evidence now has a fixed verifier. `python3 scripts/verify_goal_evidence.py` reads the latest packaged-app URL import proof, current visual GUI proof, release-readiness proof, secret-hygiene proof, and UI-hygiene proof, maps them back to the three `docs/Legacy/overview.md` capability layers plus release checks, and writes a single status ledger. Latest proof: `logs/diagnostics/2026-05-26/goal-evidence-status-20260526-121834/proof.json`, with `status=local_personal_loop_verified_with_external_distribution_blockers`, `25` verified requirements, `1` personal-local degradation for live recording fallback, `2` explicitly external distribution blockers, and no missing requirement in the current local personal loop.
- Secret hygiene is now a fixed release gate instead of a manual search. `python3 scripts/verify_secret_hygiene.py` scans release-relevant source/docs/scripts/tests while excluding logs, dist/build artifacts, media, model caches, and generated Xcode state. Latest proof: `logs/diagnostics/2026-05-26/secret-hygiene-20260526-121300/proof.json`, with `status=passed`, `267` scanned files, and `0` findings for high-confidence hardcoded API keys or private keys.
- UI hygiene is now a fixed release gate instead of a manual search. `python3 scripts/verify_ui_hygiene.py` scans the macOS app Swift source for release-blocking placeholder copy, TODO/FIXME/TBD markers, empty button actions, and permanently disabled controls. Latest proof: `logs/diagnostics/2026-05-26/ui-hygiene-20260526-121736/proof.json`, with `status=passed`, `75` scanned Swift source files, and `0` findings.
- Packaged app runtime no longer invalidates its own code signature after a real import. Local release preflight exposed that the Python sidecar was generating `__pycache__/*.pyc` under `InsightKit.app/Contents/Resources/insightkit_runtime`, which changed sealed resources after signing. `PythonRuntimeEnvironment` now launches app-owned Python children with `PYTHONDONTWRITEBYTECODE=1`, `SidecarManager` and `LiveASRService` both use it, and `package_insightkit_app.sh` recursively sanitizes xattrs after staging/install copies. Focused coverage `swift test --package-path macos/InsightKitApp --filter SidecarBuildMismatchRecoveryTests` passed with `4 tests, 0 failures`; full Swift coverage `swift test --package-path macos/InsightKitApp` passed with `115 tests, 0 failures`. Rebuilt installed app build `20260526113744` passed strict codesign verification before and after `python3 scripts/run_packaged_app_url_import_smoke.py`; `find /Users/yann.jy/Applications/InsightKit.app/Contents/Resources/insightkit_runtime -name __pycache__ -type d -print` returned no output after the smoke; `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` exited `0` after the smoke with only expected Developer ID/hardened-runtime/sandbox/privacy-URL warnings.
- Current-build visual GUI proof now exists for the same installed build `20260526113744`: `logs/diagnostics/2026-05-26/current-build-visual-gui-proof-20260526-113744.json` and screenshot `logs/diagnostics/2026-05-26/current-build-visual-gui-proof-20260526-113744.png`. Computer Use opened `/Users/yann.jy/Applications/InsightKit.app`, verified the home cards and latest recent record, opened `file-013cc112-3e96-431b-a5bd-9cdba71d5bfc` from Records, verified search filtering from `34` to `3` results, and observed the restored record detail with media, export buttons, smart minutes, timestamped transcript, and time-bound note. It clicked a smart-minutes chapter, transcript row, and note row and received visible `record_media_seek_status` updates at `00:11`, `00:20`, and `00:05` respectively. This pairs the repeatable URL smoke artifact with screenshot-level GUI evidence on the latest canonical build.
- Release-readiness status table now exists at `docs/plans/2026-05-26-insightkit-release-readiness-status.md`. It uses explicit statuses (`implemented/verified`, `local-release-ready`, `personal-local-degradation`, `externally-blocked`, `needs-channel-decision`, `not-complete`) and maps `docs/Legacy/overview.md` capabilities, engineering gates, Apple distribution gates, privacy/sandbox inputs, and owner-controlled blockers. The table is grounded in the latest local preflight, Developer ID preflight, App Store preflight, Apple official docs, installed app config, and current proof artifacts.
- Repeatable packaged-app import smoke now exists without XCUITest. The app registers the `insightkit://` URL scheme and `ContentView` routes incoming URLs through `WorkflowCoordinator.handleIncomingURL(...)`; `insightkit://import?path=<encoded local media path>` validates the local media path with `SupportedMediaTypes`, opens the import workspace, and starts the same `ImportSessionViewModel.importFile(url:)` path used by the app. Added `scripts/run_packaged_app_url_import_smoke.py` to launch `/Users/yann.jy/Applications/InsightKit.app`, open the import URL, wait for the app-owned `/tmp/insightkit-app-501.sock` sidecar, discover the real import job by `source_path`, validate record artifacts/schema/SQLite FTS/export output, then quit and verify no process/socket residue. Focused Swift coverage `swift test --package-path macos/InsightKitApp --filter AppURLHandlerTests` passed with `3 tests, 0 failures`; focused Python coverage `python3 -m pytest tests/test_packaged_app_url_import_smoke.py -q` passed with `2 passed`; full Python coverage `./scripts/run_python_tests.sh` passed with `150 passed, 1 warning`. Latest packaged installed app build `20260526113744` includes `CFBundleURLTypes` for `insightkit`, passed strict codesign verification, and the smoke passed with real sample `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a`, record `/Users/yann.jy/Documents/InsightKit/Records/file-013cc112-3e96-431b-a5bd-9cdba71d5bfc`, DeepSeek final analysis provenance, 11 timestamped rows, schema-valid insight package, SQLite/FTS query `left`, Markdown/PDF exports, and clean app/sidecar/socket shutdown. Proof: `logs/diagnostics/2026-05-26/packaged-app-url-import-smoke-20260526-113806/proof.json`. This does not replace visual Computer Use evidence, but it closes the repeatable installed-app import smoke gap while local XCUITest remains blocked by macOS automation initialization.
- Records search/recovery hang is now closed on the canonical installed app. The failure was reproduced on build `20260526102135`: after relaunch, setting the Records search field to `Canonical GUI E2E` produced a Computer Use `noWindowsAvailable` state and bounded quit had to send `TERM`. Root cause was query-time disk I/O in `RecordsIndexService.searchRecords`, where SwiftUI filtering could synchronously read `transcript.json`, `minutes.json`, and `notes.md` for every record. `RecordsIndexService` now builds a lowercased `contentSearchIndex` during `refreshIndex()` and query-time filtering only reads that in-memory index. Regression coverage `swift test --package-path macos/InsightKitApp --filter RecordsIndexServiceTests` passed with `8 tests, 0 failures`, including `testSearchRecordsUsesRefreshTimeContentIndex`; `swift test --package-path macos/InsightKitApp --filter MediaSeekRequestTests` passed with `7 tests, 0 failures`; full `swift test --package-path macos/InsightKitApp` passed with `111 tests, 0 failures`; `python3 scripts/smoke_test_rpc.py` passed `12 passed, 0 failed`; full `./scripts/run_python_tests.sh` passed with `148 passed, 1 warning`; strict codesign verification passed on `/Users/yann.jy/Applications/InsightKit.app`.
- Latest canonical real GUI import/search/recovery proof: `/Users/yann.jy/Applications/InsightKit.app` build `20260526104223` imported the real sample `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a` through the app, generated record `/Users/yann.jy/Documents/InsightKit/Records/file-c1b3311a-9e9c-4ab6-b4f1-0c2632709fab`, wrote 11 timestamped `spk0`/`spk1` transcript segments, persisted DeepSeek final analysis provenance, created time-bound note `00:22 Canonical GUI E2E note 20260526`, and exported readable Markdown/PDF. Artifact checks validated `insight_package.json` schema, SQLite segment count `11`, FTS match count `1` for `register`, Markdown sections for AI disclaimer / meeting quote / speaker summary / key decision / action-items empty state / smart chapters / related links / time-bound note / timestamped transcript, and a two-page PDF. After relaunch, Computer Use searched `Canonical GUI E2E`, saw `全部记录 (1)`, restored the record detail with media, smart minutes, transcript, notes, and export buttons, clicked smart-minutes chapter / transcript / note rows without timeout, surfaced seek statuses at `00:02`, `00:20`, and `00:22`, and `scripts/dev_quit_insightkit_processes.sh` returned `InsightKit quit cleanly.` with no app process, no sidecar process, and no socket. Proof: `logs/diagnostics/2026-05-26/canonical-gui-import-search-recovery-proof.json`.
- App-side DeepSeek/provider proof is now verified through the actual macOS app path instead of terminal Keychain access. `SettingsView` uses a 30s manual provider-probe RPC timeout so a cold sidecar/provider check no longer reports the old 6s `analysis.provider.probe` timeout. Added `scripts/verify_app_side_provider.py` and `tests/test_verify_app_side_provider_script.py`; focused Python coverage `python3 -m pytest tests/test_verify_app_side_provider_script.py tests/test_verify_current_provider_script.py -q` passed with `4 passed`, focused Swift coverage `swift test --package-path macos/InsightKitApp --filter SidecarBuildMismatchRecoveryTests` passed with `3 tests, 0 failures`, and full Swift coverage `swift test --package-path macos/InsightKitApp` passed with `108 tests, 0 failures`. The installed app was rebuilt as `/Users/yann.jy/Applications/InsightKit.app` with `CFBundleVersion=20260526092324`; Computer Use opened Settings, confirmed `DeepSeek` + `https://api.deepseek.com` + `deepseek-v4-flash` + `Key: ✓ 已保存`, clicked `检查可用性`, and observed `连接通过 ... 连接成功。` at `2026年5月26日 9:24:01`. The same GUI-started sidecar reports `sidecar.version.build=20260526092324`, matching the app `CFBundleVersion`, because `SidecarManager` now passes `INSIGHTKIT_BUILD` when launching Python. App-side proof `logs/diagnostics/2026-05-26/app-side-provider-validation-proof.json` uses the GUI-saved Keychain provider config, ingests 5 rows from the real import transcript sample, probes DeepSeek successfully, and builds a strict final package with provider `deepseek`, model `deepseek-v4-flash`, speaker summaries, decisions, actions, and smart chapters.
- Human-in-loop reduction is now an explicit development artifact. Added fixed wrappers `scripts/dev_build_xcode_debug_app.sh`, `scripts/dev_open_xcode_debug_app.sh`, `scripts/dev_check_insightkit_processes.sh`, `scripts/dev_quit_insightkit_processes.sh`, and `scripts/dev_capture_insightkit_logs.sh` so repeated local validation can use narrow approved entrypoints instead of new ad hoc shell commands. Verification: `bash -n` passed for all five scripts; `scripts/dev_build_xcode_debug_app.sh` built `/private/tmp/insightkit-xcode-dev/Build/Products/Debug/InsightKitApp.app` and strict codesign verification passed; `scripts/dev_open_xcode_debug_app.sh` launched that Debug app; `scripts/dev_check_insightkit_processes.sh` observed its `InsightKitApp` process; `scripts/dev_quit_insightkit_processes.sh` quit cleanly and confirmed no residual app/sidecar/socket; `scripts/dev_capture_insightkit_logs.sh --last 1m --output /private/tmp/insightkit-dev-log-check.txt` wrote a bounded log capture. This keeps the main loop on development validation and defers package/sign/notarize work until release-specific checks.
- Latest current-build Computer Use import/recovery proof: `/Users/yann.jy/Applications/InsightKit.app` build `20260526092324` imported the real sample `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a` through the manual local-path fallback, showed bounded processing progress and a visible cancel button, generated record `/Users/yann.jy/Documents/InsightKit/Records/file-c21bf809-d079-4234-ae51-1397739f13a0`, and exposed media playback, 11 timestamped transcript rows with `spk0`/`spk1`, smart minutes sections, notes, and Markdown/PDF export buttons. Computer Use verified pre-relaunch chapter, transcript, and note seeks at about `00:11`, `00:20`, and `00:29`; created note `00:29 Latest build E2E note`; exported readable Markdown/PDF; quit cleanly in `real 1.18`; relaunched; restored the record from Home/Records; and searched `Latest build E2E` down to one result. Artifact checks verified `metadata.analysis.provider=deepseek`, `model=deepseek-v4-flash`, strict final insight package counts, SQLite segment count `11`, FTS match `register`, Markdown sections matching the overview, and a two-page PDF. Proof: `logs/diagnostics/2026-05-26/cua-latest-build-import-recovery-proof.json`.
- Previous residual from build `20260526092324`: after relaunch, record detail restored media/transcript/smart-minutes/notes/export controls, but a subsequent restored smart-chapter click caused Computer Use state capture to time out. No `InsightKitApp*.ips` crash report was found and the app process remained running, but `scripts/quit_insightkit_app.sh` had to send TERM after the 3s AppleEvent timeout and stopped the app in `real 6.83`. This is retained as the historical failure the media teardown work was designed to close.
- Media player teardown fix is now verified first in the development loop and then in the canonical installed app. `MediaPlayerView` explicitly dismantles `AVPlayerView`, weakly references the view from its coordinator, pauses/replaces the AVPlayer item on release, drops time observers before teardown, gates asynchronous callbacks after teardown, and uses minimal AVKit controls for audio-only media while retaining inline controls for video. Focused coverage `swift test --package-path macos/InsightKitApp --filter MediaSeekRequestTests` passed with `7 tests, 0 failures`; current full Swift coverage passed with `110 tests, 0 failures`; `python3 scripts/smoke_test_rpc.py` passed `12 passed, 0 failed` and stopped its temporary sidecar. Xcode-beta Debug `.app` build through the XcodeGen project succeeded at `/private/tmp/insightkit-xcode-dev/Build/Products/Debug/InsightKitApp.app` and passed strict codesign verification. Computer Use opened that Debug `.app`, restored record `file-c21bf809-d079-4234-ae51-1397739f13a0`, clicked restored smart-minutes chapter `00:11 提出方案`, received a fresh accessibility state instead of timing out, and observed `record_media_seek_status=已跳转到 00:11 · 章节：提出方案`. The canonical installed app was then rebuilt and installed as `/Users/yann.jy/Applications/InsightKit.app` with `CFBundleVersion=20260526102135`; strict codesign verification and `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` passed in local mode. Computer Use opened the installed app, restored the same record, clicked `record_minutes_chapter_row_2`, `record_transcript_row_6`, and `live_note_row_0` without timeout, and observed visible seek statuses at `00:11`, `00:12`, and `00:29`. `scripts/dev_quit_insightkit_processes.sh` then returned `InsightKit quit cleanly.` with no app process, no sidecar process, and no sidecar socket. Proofs: `logs/diagnostics/2026-05-26/media-player-teardown-xcode-debug-proof.json` and `logs/diagnostics/2026-05-26/canonical-media-teardown-recovery-proof.json`.
- Exit/wait hardening was tightened for Settings-started sidecars and stale sockets. `AppLifecycleDelegate.applicationWillTerminate` now stops the Settings shared sidecar and performs a 1s best-effort socket-owner shutdown; `SidecarManager.bestEffortShutdownSocketOwner(...)` removes stale socket files after shutdown; `scripts/quit_insightkit_app.sh` default waits are reduced to 3s app quit, 2s TERM, and 2s sidecar shutdown, detects both bundled and dev `insight_sidecar.py` paths, and removes orphaned `/tmp/insightkit-app-501.sock`. Evidence: `bash -n scripts/quit_insightkit_app.sh` passed; after a Computer Use Settings provider check, menu quit left no `InsightKitApp`, no `insight_sidecar.py`, and no sidecar socket; `scripts/quit_insightkit_app.sh` then returned in `real 0.07`. A final running-state quit after the latest app-side provider proof returned `InsightKit quit cleanly.` in `real 1.07`, followed by no app process, no sidecar process, and no socket.
- Analysis provenance is now persisted as part of each record asset: `RecordWriter.write_record(...)` accepts sanitized `analysis_meta` and writes `metadata.json.analysis` with provider/model/source/strictMode/analysisState, never API keys. Imported jobs pass `InsightService.last_call_meta` into the record folder, and live saved records pass Swift `metrics.provider` into `records.save`. Regression coverage: `python3 -m pytest tests/test_record_writer.py tests/test_record_e2e.py tests/test_transcription_runner_local_fallback.py tests/test_verify_current_provider_script.py -q` passed with `31 passed`; focused Swift coverage `swift test --package-path macos/InsightKitApp --filter LiveSessionViewModelTests/testSaveToRecordsPersistsGeneratedLiveInsightPackageForRecovery` passed; full `swift test --package-path macos/InsightKitApp` passed with `107 tests, 0 failures`; full `./scripts/run_python_tests.sh` passed with `146 passed, 1 warning in 16.41s`. Real import E2E now fails if `metadata.json` lacks analysis provenance; the latest real 30s sample run passed and produced `metadata.analysis.provider=local-extractive`, `model=heuristic-v1`, `source=final`, proving the exported record can disclose whether smart minutes came from a cloud provider or local fallback. The local app was rebuilt and installed as `/Users/yann.jy/Applications/InsightKit.app` with `CFBundleVersion=20260525215541`; strict `codesign --verify --deep --strict --verbose=2 /Users/yann.jy/Applications/InsightKit.app` passed. Proof: `logs/diagnostics/2026-05-25/real-import-e2e-20260525-215128/proof.json`.
- Current provider validation script: added `scripts/verify_current_provider.py` to read the active runtime config, perform bounded Keychain lookup, run a real provider probe/final-build when a key is available, and always validate the local extractive fallback with a real transcript sample while redacting transcript text and never logging API keys. Unit coverage: `python3 -m pytest tests/test_verify_current_provider_script.py tests/test_analysis_provider_probe.py -q` passed with `9 passed`. Current proof `logs/diagnostics/2026-05-25/current-provider-validation-proof.json` selected DeepSeek `deepseek-v4-flash`, but terminal Keychain lookup returned status `error`/returncode `44`; therefore this proof does not verify the saved GUI DeepSeek key or online DeepSeek generation. It does verify `local-extractive` fallback produces a schema-valid package from the real transcript sample.
- Swift test config isolation proof: `AppConfigStore` now accepts injected `UserDefaults` and a `configSnapshotURL`; `AppConfigStore.shared` detects XCTest/PackageTests/.xctest execution and redirects to temporary defaults plus a temporary `runtime_config_v1.json`, so Swift tests no longer overwrite the user's real runtime config snapshot. `ASREnginePersistenceTests` now constructs an isolated store instead of using `AppConfigStore.shared`. `swift test --package-path macos/InsightKitApp` passed with `107 tests, 0 failures`; the real config snapshot hash stayed `df11330d1c085c3ee371ffa9a44d7732058b6abd27ddc8fd7925ac61eb5c8c61` before and after the isolated run. The installed app was rebuilt as `CFBundleVersion=20260525213542`, strict codesign verification passed, Computer Use opened Settings and confirmed the user's runtime config was restored to DeepSeek + FunASR, then `scripts/quit_insightkit_app.sh` quit cleanly and `pgrep -xfl InsightKitApp` found no process. Proof: `logs/diagnostics/2026-05-25/swift-test-config-isolation-proof.json`.
- Settings provider switching/error-state proof: `AppConfigStore` now repairs provider profiles copied from another provider default and backfills missing provider profiles; `SettingsView` tracks the vendor currently owning the loaded fields separately from the picker selection, so switching OpenAI/DeepSeek no longer writes DeepSeek endpoint/model into OpenAI. It also clears stale provider probe/status text when switching vendors. `swift test --package-path macos/InsightKitApp --filter AppConfigStoreTests` passed with `4 tests, 0 failures`; full `swift test --package-path macos/InsightKitApp` passed with `107 tests, 0 failures`; `./scripts/package_insightkit_app.sh --install-dir /Users/yann.jy/Applications` produced installed build `20260525212632`; strict codesign verification passed. Computer Use verified OpenAI shows `https://api.openai.com/v1` / `gpt-4.1-mini`, DeepSeek shows `https://api.deepseek.com` / `deepseek-v4-flash`, switching back and forth does not stale/cross-write fields, Qwen missing-key probe surfaces an actionable `智能分析服务未配置 API Key` error, and switching back clears the stale Qwen error. The app was restored to DeepSeek/FunASR and quit cleanly; post-quit process check found no `InsightKit` processes. Proof: `logs/diagnostics/2026-05-25/settings-provider-switch-proof.json`; screenshot: `logs/diagnostics/2026-05-25/settings-provider-switch-repair.png`.
- Python unit suite: latest `./scripts/run_python_tests.sh` outside the Codex sandbox passed with `139 passed, 1 warning in 18.04s` after the RPC PDF fallback, export output-dir, export related-link fallback, record-linkage, non-interactive smoke, and real-import E2E script updates. A sandboxed run still fails only because the sandbox denies Unix socket bind in persistent-connection tests; the same suite passes outside the sandbox. A persistent-connection startup race was fixed by waiting for an actual test connection before running assertions.
- Swift suite: `swift test --package-path macos/InsightKitApp` passed with `87 tests, 0 failures` after the media seek, record review, native export, release preflight, missing-media error-state, import-error-state, ASR model error-state, sandbox records-root fixes, RPC export rendering package refresh, live-recording media persistence fix, short-recording visible status, import cancellation feedback, and import provider fallback status.
- Latest Swift regression: `swift test --package-path macos/InsightKitApp` passed with `87 tests, 0 failures` after adding explicit app/view-model shutdown hooks and tightening the bounded quit script.
- Latest post-relaunch linkage Swift regression: `swift test --package-path macos/InsightKitApp` passed with `89 tests, 0 failures` after adding visible record-detail seek status and stable record smart-minutes/transcript row accessibility ids.
- Focused Swift proof: `swift test --package-path macos/InsightKitApp --filter RecordsIndexServiceTests` passed with `7 tests, 0 failures`; `testRecordReviewShowsVisibleMissingMediaStatus` verifies missing persisted media produces a visible `媒体文件缺失` status while keeping transcript review available, `testRecordReviewLoadsTimelineBeatsAsChapters` verifies historical-record smart minutes retain highlights, key decisions, action items, and speaker-summary fallback from timestamped transcript speakers, `testFilterRecordsCombinesTagsTypeAndTimeRange` verifies tags, media type, and time range filters compose correctly, and the records-root tests verify non-sandbox, sandbox-default, and persisted custom-root sidecar alignment behavior.
- Focused import-error proof: `swift test --package-path macos/InsightKitApp --filter ImportSessionViewModelRecordFallbackTests` passed in the current `9 tests, 0 failures` suite; `testVisibleImportErrorStatusMessageIgnoresEmptyValues` verifies import/transcription errors produce a visible status string while empty values are hidden.
- Focused ASR model error proof: `swift test --package-path macos/InsightKitApp --filter ASRRuntimeStatusPresentationTests` passed with `3 tests, 0 failures`; `testMissingModelProducesActionableSettingsWarning` verifies `modelExists=false` produces a settings warning that names the missing model and repair action.
- Focused live-recording media proof: `swift test --package-path macos/InsightKitApp --filter ChunkAssemblerTests` passed with `1 test, 0 failures`; `swift test --package-path macos/InsightKitApp --filter LiveSessionViewModelTests` passed with `15 tests, 0 failures`; proof index `logs/diagnostics/2026-05-24/live-recording-media-proof.json` verifies live chunks and short pending audio are combined into `recording.wav`, exposed as `mediaURL`, passed to `records.save` with duration and timestamped notes, and that no-audio/too-short recordings surface a visible status.
- Focused export related-link proof: `python3 -m pytest tests/test_insight_coord.py` passed with `8 tests, 0 failures`; `test_document_export_keeps_related_links_without_source_path` verifies Markdown export still includes `原始记录`, `文字记录`, and `媒体回放` when a direct media source path is unavailable. Proof index: `logs/diagnostics/2026-05-24/export-related-links-proof.json`.
- Focused import cancellation/progress/provider proof: `swift test --package-path macos/InsightKitApp --filter ImportSessionViewModelRecordFallbackTests` passed with `9 tests, 0 failures`; the import processing view now exposes user-visible stage text, `import_processing_status`, a cancellable `import_cancel_button`, visible provider/API-key fallback through `import_analysis_status`, and visible cancellation-failure feedback through `import_error_status`. Proof index: `logs/diagnostics/2026-05-24/import-cancel-progress-proof.json`.
- Bounded UI automation proof: `logs/diagnostics/2026-05-24/bounded-uitest-proof.json`; `./scripts/run_uitests.sh` now regenerates the XcodeGen project by default and terminates after `INSIGHTKIT_UITEST_TIMEOUT_SEC` instead of waiting indefinitely. The latest run exited `124` after 90 seconds because Xcode failed to initialize UI automation mode with `Timed out while enabling automation mode.`, so XCUITest remains an optional evidence layer while development continues through Swift/Python tests, sidecar smoke, real artifacts, and bounded Computer Use E2E.
- Sidecar smoke: latest default `python3 scripts/smoke_test_rpc.py` passed `12 passed, 0 failed`, auto-started the temporary sidecar when the socket was unavailable, shut it down after the run, and left no `insight_sidecar.py` process. Proof: `logs/diagnostics/2026-05-25/noninteractive-sidecar-smoke-proof.json`.
- Non-interactive real import E2E: `python3 scripts/run_real_import_e2e.py` passed with the real 30s sample `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a`. It runs against an isolated DB/Records root/socket, completes `transcription.import_file`, validates the record folder, `insight_package.json` schema, SQLite/FTS search, overview-aligned Markdown/PDF export, and owned-sidecar cleanup. Proof: `logs/diagnostics/2026-05-25/real-import-e2e-20260525-105950/proof.json`.
- Computer Use record-review E2E: `/Users/yann.jy/Applications/InsightKit.app` build `20260525104413` was opened through Computer Use, restored the latest imported record, searched records with `account`, clicked smart-minutes chapter, transcript, and note rows to seek media, created a new time-bound note, exported Markdown/PDF, quit cleanly, relaunched, and restored the record, media, transcript, smart minutes, and notes. Proof: `logs/diagnostics/2026-05-25/cua-record-review-export-recovery-proof.json`.
- Computer Use import-picker E2E: `/Users/yann.jy/Applications/InsightKit.app` build `20260525114436` selected a real `m4a` file through the native import open panel, completed sidecar import, generated record `file-77c31083-0dc5-4b43-80d1-91029c9f61c9`, created a time-bound note, exported Markdown/PDF, quit/relaunched cleanly, and restored the record. The pass also verified chapter timestamp normalization: a persisted AI timestamp `01:13` on a 30s clip now displays as `00:13` and seeks to `00:13`. Proof: `logs/diagnostics/2026-05-25/cua-import-picker-timestamp-normalization-proof.json`.
- Computer Use records search/filter proof: `/Users/yann.jy/Applications/InsightKit.app` exposed real type filters, tag filters, and time filters in Records. Searching `无转写纪要` narrowed the list to one live record; selecting `本周` kept it visible, selecting `更早` excluded it and showed `records_empty_state`, toggling `更早` restored it, `视频` excluded it, and `音频` restored it. A follow-up pass searched `注册`, clicked `records_tag_filter_注册流程`, narrowed the list from 4 records to 1, opened that imported record, clicked a smart-minutes chapter to seek media to `00:12`, and exported readable Markdown/PDF files. Proof: `logs/diagnostics/2026-05-25/cua-records-search-filter-proof.json`.
- Computer Use fresh path-import proof: `/Users/yann.jy/Applications/InsightKit.app` build `20260525145409` imported the real 30s sample through the manual local-path fallback after the native m4a open panel remained unstable under Computer Use. The app generated record `file-ecd9d3c4-c69f-460a-ac98-3bea0a3c83d3` with `recording.wav`, `metadata.json`, `transcript.json`, `minutes.json`, `insight_package.json`, and `notes.md`; the GUI showed 11 timestamped transcript rows with `spk0/spk1`, a smart-minutes summary, two smart chapters, and the note `Fresh import fallback path note` at `00:30`. Chapter, transcript, and note clicks jumped playback to `00:11`, about `00:20`, and `00:30`; Markdown/PDF export succeeded; `notes.md` and the exported Markdown contain the time-bound note; the PDF is `version 1.3, 2 pages`; schema validation and SQLite/FTS search passed; quit/relaunch restored the record from Home and Records, and search `注册` narrowed records from 26 to 6. Proof: `logs/diagnostics/2026-05-25/cua-fresh-import-path-fallback-proof.json`; screenshot: `logs/diagnostics/2026-05-25/cua-fresh-import-path-fallback-restored.png`.
- Fresh focused regressions for path-import and note persistence: `swift test --package-path macos/InsightKitApp --filter ImportSessionViewModelRecordFallbackTests` passed with `10 tests, 0 failures`; `swift test --package-path macos/InsightKitApp --filter SupportedMediaTypesTests` passed with `4 tests, 0 failures`; `python3 -m pytest tests/test_record_e2e.py tests/test_record_writer.py tests/test_insight_coord.py -q` passed with `34 passed in 2.70s`.
- Manual import-path error-state proof: `SupportedMediaTypes.validateLocalMediaFileURL` now owns local path validation for empty input, missing files, directories, and unsupported extensions; `FileDropZoneView` renders the resulting user-facing error instead of submitting invalid paths to the import pipeline. `swift test --package-path macos/InsightKitApp --filter SupportedMediaTypesTests` now passes with `6 tests, 0 failures`; full `swift test --package-path macos/InsightKitApp` passes with `101 tests, 0 failures`; `./scripts/run_python_tests.sh` passes with `142 passed, 1 warning in 18.29s`. The installed app build `20260525155906` was packaged and `codesign --verify --deep --strict --verbose=2 /Users/yann.jy/Applications/InsightKit.app` reports `valid on disk` and `satisfies its Designated Requirement`. Computer Use verified that `/tmp/insightkit-not-media.txt` shows `找不到文件：/tmp/insightkit-not-media.txt`, and `/Users/yann.jy/Developer/Projects/transcription/README.md` shows `不支持的文件格式：.md。请选择 mp3、m4a、wav、mp4、mov 或 mkv。`. Proof: `logs/diagnostics/2026-05-25/cua-import-path-error-state-proof.json`; screenshot: `logs/diagnostics/2026-05-25/cua-import-path-error-state.png`.
- Export failure visible-status proof: historical record export and completed-import native export now have regression tests proving unsupported formats surface `导出失败：不支持的导出格式：docx` instead of failing silently. `swift test --package-path macos/InsightKitApp --filter RecordDocumentExporterTests` passed with `4 tests, 0 failures`; `swift test --package-path macos/InsightKitApp --filter ImportSessionViewModelRecordFallbackTests` passed with `11 tests, 0 failures`; full `swift test --package-path macos/InsightKitApp` passed with `103 tests, 0 failures`; related Python export/record tests `python3 -m pytest tests/test_insight_coord.py tests/test_record_e2e.py tests/test_record_writer.py -q` passed with `34 passed in 2.46s`. Proof: `logs/diagnostics/2026-05-25/export-failure-visible-status-proof.json`.
- Manual import unreadable-file proof: `SupportedMediaTypes.validateLocalMediaFileURL` now detects supported media paths that exist but are not readable and returns `无法读取文件：<path>。请检查文件权限或重新选择。` before the sidecar import path is called. `swift test --package-path macos/InsightKitApp --filter SupportedMediaTypesTests` passed with `7 tests, 0 failures`; full `swift test --package-path macos/InsightKitApp` passed with `104 tests, 0 failures`; related Python import/record tests `python3 -m pytest tests/test_transcription_import_rpc.py tests/test_record_e2e.py tests/test_record_writer.py -q` passed with `27 passed in 0.33s`. The installed app build `20260525160626` passed strict codesign verification. Computer Use verified `/private/tmp/insightkit-unreadable.m4a` with `chmod 000` shows `无法读取文件：/tmp/insightkit-unreadable.m4a。请检查文件权限或重新选择。`; the temp fixture permission was restored after validation. Proof: `logs/diagnostics/2026-05-25/cua-import-path-unreadable-state-proof.json`; screenshot: `logs/diagnostics/2026-05-25/cua-import-path-unreadable-state.png`.
- Current release-readiness proof: installed app build `20260525160626` has bundle id `com.yannjy.insightkit`, version `0.1.0`, packaged icon, microphone/camera/screen-capture usage descriptions, strict codesign verification, and Apple Development local signing. `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` exits `0` for the local channel with warnings for public distribution gaps. Strict `--developer-id` preflight exits `1` for missing Developer ID identity/signature, missing hardened runtime, and non-clean Gatekeeper proof. Strict `--app-store` preflight exits `1` for missing Mac App Store distribution identity/signature, missing embedded App Sandbox/file-access entitlements in the packaged app, and missing privacy policy URL. Source App Store entitlements exist and include App Sandbox, user-selected read/write file, app-scoped security bookmark, and network-client entitlements. The source/docs/scripts/tests secret scan found no common hardcoded API/private-key patterns after excluding logs, dist, build outputs, media, archives, and images. `./scripts/release_build.sh --version 0.1.0` passed and produced local/internal QA zip `dist/releases/InsightKit-0.1.0-macos-local-20260525161028.zip` plus preflight log `dist/releases/InsightKit-0.1.0-local-20260525161028-preflight.txt`; the archive contains `InsightKit.app/Contents/MacOS/InsightKitApp`, `InsightKit.icns`, bundled `insightkit_runtime/`, schema files, sidecar scripts, and code signature resources. Apple docs rechecked on 2026-05-25 for App Sandbox, Developer ID/notarization, App Store privacy policy URL, and sandboxed file access requirements. Proof: `logs/diagnostics/2026-05-25/release-readiness-current-build-proof.json`.
- Current-source sidecar/real-import refresh and non-silent runner proof: `python3 scripts/smoke_test_rpc.py` passed `12 passed, 0 failed`, covering short-connection RPC, persistent handshake, sidecar status/version/readiness, ASR runtime status, provider status, diagnostics, capabilities, and transcription status, then stopped its temporary sidecar. `scripts/run_real_import_e2e.py` now prints timestamped progress heartbeats while polling the import job and exposes `--progress-interval-sec`; `python3 -m py_compile scripts/run_real_import_e2e.py` passed. A real run with `python3 scripts/run_real_import_e2e.py --progress-interval-sec 15 --timeout-sec 180` emitted bounded heartbeat lines at 0s/15s/30s/45s/60s/75s/90s, then completed at 100% instead of staying silent. It used the real 30s sample `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a`, producing record `logs/diagnostics/2026-05-25/real-import-e2e-20260525-162100/Records/file-833d58e5-bfc1-49cf-9c65-1e7ec8351624`, Markdown export `logs/diagnostics/2026-05-25/real-import-e2e-20260525-162100/exports/file-833d58e5-bfc1-49cf-9c65-1e7ec8351624_1779697357.md`, and PDF export `logs/diagnostics/2026-05-25/real-import-e2e-20260525-162100/exports/file-833d58e5-bfc1-49cf-9c65-1e7ec8351624_1779697357.pdf` (`PDF document, version 1.7`). The proof validates completed job state, 5 timestamped rows, `SPEAKER_00`/`SPEAKER_01` speaker labels, insight schema, SQLite/FTS query `left`, overview-aligned Markdown sections, PDF `%PDF-` header, and owned sidecar cleanup. The initial silent run was monitored with bounded polling, proof-directory checks, pgrep checks, and sidecar log inspection rather than waiting indefinitely. Proof: `logs/diagnostics/2026-05-25/current-build-sidecar-real-import-proof.json` and `logs/diagnostics/2026-05-25/real-import-e2e-20260525-162100/proof.json`.
- Current-source Computer Use GUI import/recovery proof: installed app build `20260525163616` imported the real diagnostic media through the manual local-path fallback using `logs/diagnostics/e2e/realsample2026511en130s.m4a`, a copy of the real sample made only because Computer Use dropped underscores while typing the original filename. The packaged app generated record `/Users/yann.jy/Documents/InsightKit/Records/file-21d38f52-656d-4a31-9362-1a76dee594ef` with `recording.m4a`, `metadata.json`, `transcript.json`, `minutes.json`, `insight_package.json`, `notes.md`, and exports. The completed import no longer showed the stale empty-transcript review state: transcript rows and smart minutes became usable as soon as the import completed. Computer Use created the time-bound note `CUA safe path import note`, exported Markdown/PDF, quit cleanly, relaunched, restored the record from Home/Records, verified chapter, transcript, and note clicks seek the media to `00:00`, about `00:12`, and `00:29`, then searched `CUA safe` and narrowed the Records list from 28 records to 1. The Markdown export contains meeting topic/time/participants, AI disclaimer, quotes, speaker summary, key decisions, action items, smart chapters, related links, the note, and timestamped transcript; the PDF export is `PDF document, version 1.3, 2 pages`. Proof: `logs/diagnostics/2026-05-25/cua-gui-safe-import-fast-review-proof.json`; screenshots: `logs/diagnostics/2026-05-25/cua-gui-safe-import-restored-record.png`, `logs/diagnostics/2026-05-25/cua-gui-safe-import-search-proof.png`.
- Current-source accessibility cleanup proof: `TimestampNotesEditor` now hides UI-test-only note count/draft/submit probe Text nodes from the real app accessibility tree unless `INSIGHTKIT_UI_TEST_MODE=1`, so record seek status is no longer read as `1 0 已跳转到...` in Computer Use. `swift test --package-path macos/InsightKitApp` passed `105 tests, 0 failures`; `./scripts/package_insightkit_app.sh` produced installed build `20260525204843`; strict codesign verification passed; Computer Use restored the latest imported record and verified note seek status is exposed through `record_media_seek_status` without leading probe values; `scripts/quit_insightkit_app.sh` quit cleanly and process checks found no `InsightKitApp` or `insight_sidecar.py`. Proof: `logs/diagnostics/2026-05-25/cua-accessibility-probe-cleanup-proof.json`; screenshot: `logs/diagnostics/2026-05-25/cua-accessibility-probe-cleanup.png`.
- Live recording same-file regression fix: Computer Use found a live-recording finalize bug on build `20260525204843`: after the first `records.save` hardlinked `recording.wav` into the record folder, generating final minutes caused a second save over the same inode and surfaced `Insight 侧车错误: ... recording.wav ... are the same file`. `RecordWriter._copy_or_hardlink` now treats already-hardlinked source/destination media as idempotent. Regression coverage: `python3 -m pytest tests/test_record_writer.py -q` passed `18 passed in 0.16s`; `python3 -m pytest tests/test_record_e2e.py tests/test_insight_coord.py -q` passed `17 passed in 1.86s`; `python3 scripts/smoke_test_rpc.py` passed `12 passed, 0 failed`. After repackaging/installing build `20260525205845`, Computer Use reran a bounded live recording, observed the visible no-transcript warning, created note `Live samefile regression note` at `00:24`, stopped at about `00:31`, generated fallback minutes (`没有会议转写内容。`) without the same-file error, clicked the note to seek media to about `00:24`, quit/relaunched cleanly, restored the record from Home/Records, and exported Markdown/PDF. Record folder: `/Users/yann.jy/Documents/InsightKit/Records/live-F749DC78-066E-459E-9BB6-7FDEB010AA02`; proof: `logs/diagnostics/2026-05-25/live-recording-samefile-fix-proof.json`; screenshots: `logs/diagnostics/2026-05-25/cua-live-samefile-regression-fixed.png`, `logs/diagnostics/2026-05-25/cua-live-samefile-regression-restored-exported.png`.
- Real media import E2E:
  - Source sample: `logs/diagnostics/e2e/real_sample_2026_5_11_en_1_30s.m4a`
  - Result: `logs/diagnostics/e2e/real_import_e2e_result.json`
  - Record folder: `logs/diagnostics/e2e/Records/file-14aea501-a77e-4002-bb2d-bb4fa9f4af8c`
  - Markdown export: `logs/diagnostics/e2e/exports/file-14aea501-a77e-4002-bb2d-bb4fa9f4af8c_1779521324.md`
- PDF export:
  - Test: `tests/test_insight_coord.py::TestInsightCoordinator::test_document_export_pdf`
  - Real E2E export: `logs/diagnostics/e2e/exports/file-14aea501-a77e-4002-bb2d-bb4fa9f4af8c_1779522162.pdf`
  - Result JSON: `logs/diagnostics/e2e/real_import_pdf_export_result.json`
  - App live/transcription/import record export prefers native Swift/AppKit PDF generation through `RecordDocumentExporter` when a persisted record folder exists.
  - RPC `document.export` uses WeasyPrint when available, and now falls back to a built-in Unicode text PDF writer when WeasyPrint is unavailable.
  - RPC export renderer now maps the document body to the `overview.md` release surface: `会议信封`, `长文版结构化总结`, `会议金句`, `发言人总结`, `关键决策`, `待办事项`, `智能章节`, and `相关链接`; the old `交互占位提示` section was removed.
  - RPC export proof using a real timestamped transcript sample: `logs/diagnostics/2026-05-24/rpc-export-rendering-proof/proof.json`; generated Markdown `logs/diagnostics/2026-05-24/rpc-export-rendering-proof/real-sample-rpc-export_1779630809.md` and PDF `logs/diagnostics/2026-05-24/rpc-export-rendering-proof/real-sample-rpc-export_1779630809.pdf` include the header, AI disclaimer, structured summary, related links, media file link, and `%PDF-` header.
  - Built-in PDF fallback proof: `logs/diagnostics/2026-05-24/pdf-fallback-proof/proof.json`; generated PDF `logs/diagnostics/2026-05-24/pdf-fallback-proof/real-sample-pdf-fallback_1779631272.pdf` was produced from a real timestamped transcript sample while WeasyPrint was forced missing, and has a `%PDF-` header plus the built-in CJK font marker.
  - RPC export output-dir proof: `logs/diagnostics/2026-05-24/export-output-dir-proof/proof.json`; generated Markdown exports from the same real timestamped transcript sample prove relative `output_dir` resolves under the sidecar working directory instead of silently falling back to `~/Documents/InsightKit/exports`, while legacy `output_dir="txt"` still maps to the default export root.
  - Export related-link fallback proof: `logs/diagnostics/2026-05-24/export-related-links-proof.json`; when no direct media `source_path` is available, Markdown export still includes original record, transcript, and media playback references instead of rendering the empty-link fallback.
- macOS package:
  - Canonical installed app: `/Users/yann.jy/Applications/InsightKit.app`
  - Verification: `codesign --verify --deep --strict --verbose=2 /Users/yann.jy/Applications/InsightKit.app` reports `valid on disk` and `satisfies its Designated Requirement`.
  - Latest installed bundle metadata: `CFBundleIdentifier=com.yannjy.insightkit`, `CFBundleShortVersionString=0.1.0`, `CFBundleVersion=20260525213542`.
  - Earlier GUI/linkage proof builds `20260524183227`, `20260524185320`, and `20260524190413` are retained as historical evidence, not the current canonical install.
  - App icon: `CFBundleIconFile=InsightKit.icns`; `/Users/yann.jy/Applications/InsightKit.app/Contents/Resources/InsightKit.icns` exists and `file` identifies it as a Mac OS X icon.
  - `plutil -lint` passed for both `macos/InsightKitApp/InsightKitApp-Info.plist` and the installed app `Contents/Info.plist`.
  - Launch check: Computer Use launched and bound to `/Users/yann.jy/Applications/InsightKit.app` with bundle id `com.yannjy.insightkit`.
  - Artifact policy update: intermediate packages, logs, exports, screenshots, and proof files are retained until phase-end cleanup unless a stale artifact directly blocks verification.
- Security / release scan:
  - Secret pattern scan over repo source, excluding build/log artifacts, found no matches for common hardcoded API key or private-key patterns.
  - Current canonical app is signed with `Apple Development: yann.jy@icloud.com (LMWQNG6538)` for local validation.
  - `codesign -dvvv --entitlements :- /Users/yann.jy/Applications/InsightKit.app` shows no embedded entitlements for the manual SwiftPM bundle; this is expected for the current local package and remains a release-channel decision.
  - `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` reports Developer ID missing, Apple Development present, strict codesign passed, hardened runtime absent, local Gatekeeper override, `notarytool` available, and App Sandbox disabled.
  - `scripts/release_build.sh` now separates local/internal QA zips from Developer ID distribution attempts. Latest `./scripts/release_build.sh --version 0.1.0` passed and produced `dist/releases/InsightKit-0.1.0-macos-local-20260524221747.zip` with preflight log `dist/releases/InsightKit-0.1.0-local-20260524221747-preflight.txt`.
  - The latest local release staging app `/private/tmp/insightkit-release-0.1.0-local.ZHnide/app/InsightKit.app` passed `codesign --verify --deep --strict --verbose=2`; its verified installed `CFBundleVersion` is `20260524221829`, and `unzip -l` confirmed the archive contains `InsightKit.app/Contents/MacOS/InsightKitApp`, `InsightKit.icns`, bundled `insightkit_runtime/`, updated RPC export renderer/coordination code, sidecar/schema files, and `Info.plist`.
  - `./scripts/release_build.sh --version 0.1.0 --developer-id` fails fast on this machine because no `Developer ID Application` identity is installed.
  - `scripts/release_preflight.sh` now supports strict `--developer-id` and `--app-store` channels. Local mode exits `0` with release gaps as warnings; Developer ID mode exits `1` for missing Developer ID identity/signature, hardened runtime, and clean Gatekeeper evidence; App Store mode exits `1` because App Sandbox is disabled.
  - `scripts/notarize_insightkit_release.sh --help` passed. The script requires `scripts/release_preflight.sh --developer-id <app>` before contacting Apple, creates a notary submission zip, runs `xcrun notarytool submit --wait`, staples with `xcrun stapler`, validates codesign/Gatekeeper/preflight, and creates a final `macos-developer-id-notarized-stapled-*` public zip.
  - `scripts/notarize_insightkit_release.sh --app /private/tmp/insightkit-release-0.1.0-local.aWlXHr/app/InsightKit.app --keychain-profile insightkit-notary` exits before upload at Developer ID preflight because the current app is local QA signed and this machine has no Developer ID identity.
  - `docs/release-privacy-sandbox.md` now records current data locations, optional cloud-provider disclosure boundary, media permissions, sandbox gap, and manual App Store owner tasks.
  - `docs/release-privacy-policy-draft.md` now provides a publishable-review privacy policy draft for the local-first app boundary, and `docs/release-app-store-privacy-answers.md` records a conservative App Store privacy answer draft.
  - README's old "完全离线" claim was replaced with a local-first boundary: transcription/record management is local by default, while BYOK cloud analysis only runs after the user configures a provider/API key and may send meeting text to the selected service.
  - `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` now checks packaged media permission descriptions, privacy/sandbox release note, and privacy policy URL. Local mode exits `0` with missing privacy policy URL as `WARN`.
  - `scripts/release_preflight.sh /Users/yann.jy/Applications/InsightKit.app` also checks the privacy policy draft and App Store privacy answer draft; both pass in local and App Store channels.
  - `macos/InsightKitApp/InsightKitApp.AppStore.entitlements` now records the intended Mac App Store sandbox capabilities and passes `plutil -lint`.
  - `scripts/package_insightkit_app.sh` now accepts `--entitlements <path>` and verifies embedded true entitlements after signing; `scripts/release_preflight.sh --app-store` now checks source entitlements, packaged embedded entitlements, Mac App Store distribution signing identity/signature, and privacy policy URL.
  - `scripts/release_preflight.sh --app-store /Users/yann.jy/Applications/InsightKit.app` exits `1` because the canonical local app is not signed with a Mac App Store distribution identity, does not embed sandbox entitlements, and `INSIGHTKIT_PRIVACY_POLICY_URL` is not configured.
  - App Store sandbox/preflight proof: `logs/diagnostics/2026-05-24/app-store-sandbox-preflight-proof.json`.
  - Sandbox records-root proof: `logs/diagnostics/2026-05-24/sandbox-records-root-proof.json`; the app now uses the app container records root when sandboxed, keeps custom roots bookmark-backed, and passes the selected root to the Python sidecar through `INSIGHTKIT_RECORDS_ROOT`.
  - User-mentioned proof file `026-05-24/native-settings-import-proof.json` / `native-settings-import-proof.json` was absent in the current environment, so the current bounded GUI/import/recovery/release-setting pass was captured in `logs/diagnostics/2026-05-24/native-settings-import-proof.json`; `python3 -m json.tool` validated it.
- Packaged app UI evidence through Computer Use:
  - Fresh canonical-app proof on build `20260524183227`: Computer Use bound to `/Users/yann.jy/Applications/InsightKit.app` with bundle id `com.yannjy.insightkit`; the home screen exposed live/import/records workflow cards, recent records, and status text.
  - Fresh records proof: opening `home_card_records` exposed `records_search_field`, tag/type filters, and `records_list` with `20` records; setting query `注册` filtered the count to `2`.
  - Fresh record-detail proof: selecting `record_list_item_file-a0d91361-e00c-424f-a6aa-b5c9896fd437` restored chapter sidebar, media controls, timestamped speaker transcript rows, smart minutes summary, notes panel, and export buttons.
  - Fresh media-linkage proof: clicking chapter `live_chapter_row_1` jumped the player to `00:12` (`timeline ~= 11.99s`); clicking transcript row `00:19` jumped to `00:20` (`timeline ~= 19.63s`).
  - Fresh screenshot artifacts: `logs/diagnostics/2026-05-24/insightkit-gui-record-linkage-20260524-1850-w32851.png` and `logs/diagnostics/2026-05-24/insightkit-gui-record-linkage-20260524-1850-w32914.png`.
  - Fresh smart-minutes overview proof on installed build `20260524185320`: opening `record_list_item_file-a0d91361-e00c-424f-a6aa-b5c9896fd437` exposed `record_smart_minutes_overview` with `总结`, `会议金句`, `发言人总结`, `关键决策`, `待办事项`, `智能章节`, followed by `record_transcript_title`.
  - Fresh smart-minutes linkage proof: clicking the overview chapter button `00:12 提出方案` jumped the media player to `00:12` (`timeline ~= 11.9888s`).
  - Fresh smart-minutes screenshot: `logs/diagnostics/2026-05-24/insightkit-smart-minutes-overview-20260524-1856.png`.
  - Fresh smart-minutes structured proof: `logs/diagnostics/2026-05-24/smart-minutes-overview-proof.json`.
  - Latest canonical launch smoke on build `20260524221829`: Computer Use bound to `/Users/yann.jy/Applications/InsightKit.app` with bundle id `com.yannjy.insightkit`, pid `40215`, and window title `InsightKit`; the home screen exposed `home_title`, `home_card_live`, `home_card_import`, `home_card_records`, `home_recent_title`, recent records, and status text.
  - Latest canonical release/install proof JSON: `logs/diagnostics/2026-05-24/latest-release-install-proof.json`.
  - Latest development-first GUI import/recovery proof on build `20260525102729`: `logs/diagnostics/2026-05-25/latest-dev-gui-import-recovery-proof.json`.
  - Real audio sample imported through the packaged app from `/Users/yann.jy/Downloads/TencentMeeting/2025-09-12 13.30.50 變的快速会议 270371583/InsightKit-GUI-E2E-30s.m4a`.
  - Persisted record folder: `/Users/yann.jy/Documents/InsightKit/Records/file-a45e1e78-6d60-420d-a80b-56490e2d9788`; `transcript.json` contains 11 timestamped speaker segments, `minutes.json` contains structured summary, 2 highlights, 1 key decision, 1 action item, and 3 timeline beats, and `notes.md` contains `00:29 00:30 GUI latest build note`.
  - App-generated Markdown export: `/Users/yann.jy/Documents/InsightKit/Records/file-a45e1e78-6d60-420d-a80b-56490e2d9788/exports/file-a45e1e78-6d60-420d-a80b-56490e2d9788-20260524-231850.md`, verified with the overview-aligned release sections.
  - App-generated PDF export: `/Users/yann.jy/Documents/InsightKit/Records/file-a45e1e78-6d60-420d-a80b-56490e2d9788/exports/file-a45e1e78-6d60-420d-a80b-56490e2d9788-20260525-095356.pdf`, verified as a 2-page PDF with a Quick Look preview artifact at `/private/tmp/insightkit-pdf-preview/file-a45e1e78-6d60-420d-a80b-56490e2d9788-20260525-095356.pdf.png`.
  - Reopen/recovery verification: after reinstall and relaunch, the home screen listed the latest record; opening it through Records restored smart-minutes overview, timestamped transcript, note, media duration, and export buttons.
  - Linkage evidence caveat: chapter/transcript/note seek was verified in the import-review session before relaunch; post-relaunch media and smart-minutes restored, but a post-relaunch smart-chapter click caused Computer Use state capture to time out, so post-relaunch click-to-seek remains a weaker evidence item.
  - Latest post-relaunch record linkage proof on build `20260525104413`: `logs/diagnostics/2026-05-25/post-relaunch-record-linkage-proof.json`.
  - Record detail now exposes stable ids for `record_smart_minutes_overview`, `record_minutes_chapter_row_1`, `record_transcript_row_6`, and `live_note_row_0`.
  - Post-relaunch smart-minutes chapter click: `record_minutes_chapter_row_1` jumped the media player to `00:11` and surfaced `已跳转到 00:11 · 章节：提出解决方案`.
  - Post-relaunch transcript click: `record_transcript_row_6` jumped the media player to `00:12` and surfaced `已跳转到 00:12 · 逐字稿：spk1`.
  - Post-relaunch note click: `live_note_row_0` jumped the media player to `00:29` and surfaced `已跳转到 00:29 · 笔记：00:30 GUI latest build note`.
  - The previous post-relaunch click-to-seek evidence gap is now closed for chapter, transcript, and note interactions.
  - Home screen recovered recent records from disk after relaunch; latest imported record appears as `file-6ae4703f-7498-494c-847e-785b7be1ca51`.
  - Records search field filtered `18` records to `1` result for query `API`.
  - Record detail showed chapter sidebar, media playback surface, timestamped speaker transcript, smart minutes summary, and notes panel.
  - Created a time-bound note through the app; `~/Documents/InsightKit/Records/file-6ae4703f-7498-494c-847e-785b7be1ca51/notes.md` contains `00:00 Computer Use 验证笔记`.
  - Historical record export buttons are visible in the app: `record_export_markdown_button` and `record_export_pdf_button`.
  - App-generated Markdown export: `/Users/yann.jy/Documents/InsightKit/Records/file-6ae4703f-7498-494c-847e-785b7be1ca51/exports/file-6ae4703f-7498-494c-847e-785b7be1ca51-20260523-193606.md`.
  - App-generated PDF export: `/Users/yann.jy/Documents/InsightKit/Records/file-6ae4703f-7498-494c-847e-785b7be1ca51/exports/file-6ae4703f-7498-494c-847e-785b7be1ca51-20260523-193615.pdf`, verified as a PDF document, version 1.3, 8 pages, with `%PDF-` header.
- Packaged app GUI import evidence through Computer Use:
  - Real audio sample imported through the packaged app from `/Users/yann.jy/Downloads/TencentMeeting/2025-09-12 13.30.50 變的快速会议 270371583/InsightKit-GUI-E2E-30s.m4a`; the temporary copied sample was removed after validation.
  - Persisted record folder: `/Users/yann.jy/Documents/InsightKit/Records/file-a0d91361-e00c-424f-a6aa-b5c9896fd437`
  - Persisted artifacts verified in that folder: `metadata.json`, `recording.m4a`, `transcript.json`, `notes.md`, `minutes.json`, `insight_package.json`, and `exports/`.
  - App review state showed smart chapters, media player, timestamped transcript rows, smart minutes summary, and note panel after import completion.
  - Time-bound note created through the app: `/Users/yann.jy/Documents/InsightKit/Records/file-a0d91361-e00c-424f-a6aa-b5c9896fd437/notes.md` contains `00:29 GUI import note at playback time`.
  - App-generated Markdown export: `/Users/yann.jy/Documents/InsightKit/Records/file-a0d91361-e00c-424f-a6aa-b5c9896fd437/exports/file-a0d91361-e00c-424f-a6aa-b5c9896fd437-20260523-213836.md`.
  - App-generated PDF export: `/Users/yann.jy/Documents/InsightKit/Records/file-a0d91361-e00c-424f-a6aa-b5c9896fd437/exports/file-a0d91361-e00c-424f-a6aa-b5c9896fd437-20260523-213851.pdf`, verified as a PDF document with `%PDF-` header.
  - Reopen/recovery verification: after reinstall and relaunch, the home screen listed this imported record in recent records; opening it from records restored chapters, transcript, note, media reference, and export buttons.
  - Media linkage verification after reinstall: clicking chapter `live_chapter_row_1` jumped the player to `00:12` (`timeline ~= 11.99s`); clicking transcript row `00:19` jumped to `00:20` (`timeline ~= 19.63s`); clicking note `00:29` jumped to `00:29` (`timeline ~= 28.98s`).
- Missing media error-state evidence:
  - Historical record review now sets an explicit `媒体文件缺失` message when no `recording.*` exists in the persisted record folder.
  - The review UI exposes this state through accessibility id `record_media_missing_status` instead of silently rendering an empty playback area.
  - Swift coverage: `RecordsIndexServiceTests.testRecordReviewShowsVisibleMissingMediaStatus`.
- Import error-state evidence:
  - Import/transcription failures now remain visible on the import page instead of only resetting to the file picker.
  - The import UI exposes this state through accessibility id `import_error_status`.
  - Swift coverage: `ImportSessionViewModelRecordFallbackTests.testVisibleImportErrorStatusMessageIgnoresEmptyValues`.
- ASR model error-state evidence:
  - Settings now converts `ASRRuntimeStatus.modelExists=false` into an explicit `ASR 模型文件缺失` warning with the selected model name, lookup path, and “一键修复语音识别” action hint.
  - The settings UI exposes this state through accessibility id `settings_asr_model_missing_status`.
  - Swift coverage: `ASRRuntimeStatusPresentationTests.testMissingModelProducesActionableSettingsWarning`.
- Crash fix evidence:
  - Observed crash report: `/Users/yann.jy/Library/Logs/DiagnosticReports/InsightKitApp-2026-05-23-214926.ips`.
  - Root cause: `MediaPlayerView.Coordinator.seekIfNeeded(_:)` synchronously called the view model's `onSeek`, creating a programmatic seek feedback loop.
  - Fix: programmatic seeks now apply `AVPlayer.seek` once per `MediaSeekRequest.id` and asynchronously update `onTimeUpdate` instead of recursively calling `onSeek`.
  - Additional fix: historical record center view now observes `RecordReviewDataSource`, so chapter/transcript/note taps actually refresh `MediaPlayerView.seekRequest`.
  - Post-fix bounded checks: after chapter/transcript/note clicks, `pgrep` still showed the installed app process running and `~/Library/Logs/DiagnosticReports` showed no new `InsightKitApp*.ips` beyond the old 21:49 report.
  - Latest post-install restart: `open /Users/yann.jy/Applications/InsightKit.app` launched build `20260524004212`; process list showed `/Users/yann.jy/Applications/InsightKit.app/Contents/MacOS/InsightKitApp`, and crash reports still showed no new `InsightKitApp*.ips`.
- Exit/wait fix evidence:
  - `SidecarManager` no longer ranks Python binaries synchronously during app initialization; candidate ranking is deferred to first `startIfNeeded`.
  - `scripts/quit_insightkit_app.sh` checks for an existing `InsightKitApp` process before sending AppleEvents, then uses bounded AppleEvent/TERM/KILL fallback.
  - Running-state quit proof: `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.55`; unified log showed `Checking whether app should terminate` and `terminate:`.
  - Fresh running-state quit proof on build `20260524183227`: `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.70`; follow-up `pgrep -fl InsightKit` found no residual InsightKit process.
  - Fresh crash-report proof: `find /Users/yann.jy/Library/Logs/DiagnosticReports -maxdepth 1 -name 'InsightKitApp*' -print` returned no matching current crash reports after the GUI linkage check.
  - Fresh running-state quit proof on build `20260524185320`: `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.82`; follow-up `pgrep -fl InsightKit` found no residual InsightKit process.
  - Latest running-state quit proof on build `20260524221829`: `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.56`; follow-up `pgrep -fl InsightKit` found no residual InsightKit process.
  - Latest shutdown hardening on build `20260525102729`: `ContentView` now calls `WorkflowCoordinator.shutdown()` on `NSApplication.willTerminateNotification`; live/transcription/import view models explicitly stop their `SidecarManager`; `scripts/quit_insightkit_app.sh` now uses bundle-id AppleScript first, checks sidecar processes, and cleans them with bounded shutdown/TERM/KILL fallback.
  - Latest minimal quit proof: `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.59`; follow-up `pgrep -xfl InsightKitApp` and `pgrep -fl insight_sidecar.py` returned no output.
  - A record-detail interaction still produced one intermediate bounded quit fallback (`InsightKit did not quit within 5s; sending TERM to: 41591`) before the bundle-id script fix; this is tracked in `logs/diagnostics/2026-05-25/latest-dev-gui-import-recovery-proof.json` and keeps post-relaunch interaction hardening open.
  - Latest post-linkage quit proof on build `20260525104413`: after chapter/transcript/note click checks, `/usr/bin/time -p scripts/quit_insightkit_app.sh` returned `InsightKit quit cleanly.` in `real 0.57`; follow-up process checks found no `InsightKitApp` or `insight_sidecar.py` process.
  - Raw executable launch is not a valid GUI proof from Codex: running `/Users/yann.jy/Applications/InsightKit.app/Contents/MacOS/InsightKitApp` directly from the Codex sandbox created `/Users/yann.jy/Library/Logs/DiagnosticReports/InsightKitApp-2026-05-24-185447.ips` with `parentProc=codex` and an AppKit registration abort. Use `open /Users/yann.jy/Applications/InsightKit.app` plus Computer Use/process evidence instead.
- XCUITest note:
  - `./scripts/run_uitests.sh --regenerate` built the Xcode UI test host but failed before running tests because macOS LocalAuthentication returned `Code=-2 "Canceled by user"` while initializing UI testing. Result bundle: `/Users/yann.jy/Library/Developer/Xcode/DerivedData/InsightKitUITestHost-hhmkoxgjiczzvqczfkkgqwnxcpnr/Logs/Test/Test-InsightKitApp-2026.05.23_16-50-28-+0800.xcresult`.
  - Fresh bounded runner proof: `./scripts/run_uitests.sh` regenerated the Xcode project and exited `124` after 90 seconds with log `/tmp/insightkit_uitest.log` and result bundle `/tmp/insightkit_uitest_20260524225458.xcresult`; failure text was `Timed out while enabling automation mode.` This is tracked as local UI automation initialization failure, not an InsightKit app-flow regression.

## Overview.md Capability Mapping

### 1. AI Summary View

- Summary: implemented through `session_overview` and Swift workbench.
- Meeting quotes: implemented through `highlight_insights`.
- Speaker summaries: implemented through `speaker_perspectives`; historical record review uses a conservative speaker-label fallback when persisted `minutes.json` has only local flattened minutes fields.
- Key decisions: implemented through `decision_ledger`.
- Action items: implemented through `action_tracks`.
- Smart chapters: implemented through `timeline_beats` and sidebar chapters.
- Current evidence: Python schema tests, real media E2E insight package keys, Swift tests, and installed-app Computer Use proof that the historical record detail shows all six smart-minutes sections in `record_smart_minutes_overview`.

### 2. Minutes Body / Export View

- Markdown export: implemented and verified with real E2E artifact.
- PDF export: implemented through native Swift persisted-record export, WeasyPrint-backed RPC export when available, and built-in sidecar PDF fallback when WeasyPrint is missing; verified by `%PDF-` headers and real E2E/fallback artifacts.
- Historical record export: implemented locally from persisted record folders and verified through Computer Use in the packaged app for Markdown and PDF.
- Header/disclaimer: present in export as title and AI review notice.
- Related links: supported through `provenance_links` plus meeting/source fallback; exports now retain `原始记录`, `文字记录`, and `媒体回放` references even when a direct media source path is unavailable.
- Export path predictability: RPC `document.export` now honors caller-provided relative output directories and keeps old `"txt"` clients compatible with the default export root; proof is in `logs/diagnostics/2026-05-24/export-output-dir-proof/proof.json`.
- Current limitation: sidecar export metadata is still minimal for some imported media. Historical record export now derives meeting time, participants from speaker labels, media reference, transcript, notes, and chapter data from the local record folder.

### 3. Raw Record / Transcript Linkage

- Audio/video import: implemented through Swift file importer and `transcription.import_file`; verified with real `.m4a` sample.
- Realtime recording fallback: implemented for local live sessions by retaining live WAV chunks, flushing short pending audio, combining the result into `recording.wav` on stop before chunk cleanup, setting the review `mediaURL`, and saving that file through `records.save` as the live media asset. If no saveable audio was captured, the live center now shows a `live_recording_status` warning instead of silently rendering an empty player. Generated live smart minutes are now saved back into the record folder after the post-session Generate Minutes action, so no-transcript fallback summaries survive restart and export instead of leaving `minutes.json` as `{}`. Current proofs: `logs/diagnostics/2026-05-24/live-recording-media-proof.json`, `logs/diagnostics/2026-05-25/cua-live-recording-boundary-proof.json`, and `logs/diagnostics/2026-05-25/live-final-minutes-persistence-proof.json`.
- Timestamp transcript: persisted in SQLite and record folders; verified in E2E record output.
- Speaker labels: present when diarization is available; conservative degradation is `未标注` / generated speaker labels.
- Media jump: Swift `MediaSeekRequest` now drives `AVPlayer.seek` when transcript rows, chapters, or notes are clicked. Covered by `MediaSeekRequestTests` and verified in the packaged app with chapter, transcript, and note clicks.
- Search/filter/records: `RecordsIndexService` indexes records and searches metadata/transcript/minutes/notes; tests cover record search.
- Notes bound to time: notes use current playback/recording time and are persisted to `notes.md` in import/record review paths. Computer Use verified note creation and file persistence.
- Home recovery: home now shows recent records from `RecordsIndexService.recentRecords(limit:)` instead of a placeholder.

## Publishing / Release Readiness

Apple documentation checked:

- Mac App Store distribution requires App Sandbox to be enabled: <https://developer.apple.com/documentation/security/app_sandbox>
- App Sandbox is required for Mac App Store distribution and constrains protected user resources: <https://developer.apple.com/documentation/security/protecting-user-data-with-app-sandbox>
- Sandboxed apps should use user-selected file access and security-scoped bookmarks for persistent file access: <https://developer.apple.com/documentation/security/app_sandbox/accessing_files_from_the_macos_app_sandbox>
- Apple current sandbox file-access guidance covers temporary user-selected file access and explicit security-scoped bookmarks for persisted access: <https://developer.apple.com/documentation/security/accessing-files-from-the-macos-app-sandbox>
- Developer ID distribution requires Developer Program membership, Developer ID signing, and notarization: <https://developer.apple.com/support/developer-id/>
- Gatekeeper direct distribution uses Developer ID signing, notarytool uploads, and stapler tickets: <https://developer.apple.com/developer-id/>
- Current notarization uses `notarytool`; `altool` uploads are no longer accepted: <https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution>
- Custom command-line notarization requires packaging before upload and then stapling the ticket to the distribution: <https://developer.apple.com/documentation/security/notarizing_macos_software_before_distribution/customizing_the_notarization_workflow>
- App Store privacy details must be completed in App Store Connect and kept accurate: <https://developer.apple.com/app-store/app-privacy-details/>
- App Store Connect requires a privacy policy URL for macOS apps: <https://developer.apple.com/help/app-store-connect/reference/app-information/app-information>

Current project state:

- Bundle ID: `com.yannjy.insightkit`.
- Packaged version: `CFBundleShortVersionString=0.1.0`, generated build number.
- App icon is now present in source at `macos/InsightKitApp/Resources/InsightKit.icns` and packaged as `Contents/Resources/InsightKit.icns`.
- Usage descriptions: microphone, screen capture, and camera strings are present in the packaged `Info.plist`.
- Manual SwiftPM bundle currently uses local signing for local verification; current canonical artifact is Apple Development signed.
- `macos/InsightKitApp/InsightKitApp.entitlements` has `com.apple.security.app-sandbox` set to `false`.
- Packaged SwiftPM app currently has no embedded entitlements in the manual bundle signature.
- Output under the Desktop/FileProvider-backed repo path can get root FinderInfo/FileProvider xattrs reattached by macOS, so strict code-sign verification is stable when using `/private/tmp/insightkit-package/InsightKit.app` or the canonical local app `/Users/yann.jy/Applications/InsightKit.app`.
- The packaged app must not write into `Contents/Resources` at runtime. Current build `20260526113744` disables Python bytecode writes for app-owned sidecar/ASR processes and remains strict-codesign-valid after a real URL import smoke.
- Current machine has Apple Development signing identities but no `Developer ID Application` identity; Developer ID packaging fails fast with a clear error rather than silently falling back.
- `scripts/release_build.sh` produces timestamped local/internal QA zips named `macos-local-*`; Developer ID mode is explicitly separate and labels its future output `macos-developer-id-unnotarized-*` until notarization/stapling is implemented and verified.
- `scripts/release_preflight.sh --developer-id` and `scripts/release_preflight.sh --app-store` provide failing release gates instead of warn-only local checks.
- `scripts/notarize_insightkit_release.sh` provides the notarization/stapling path but cannot complete until a Developer ID signed app and notarytool credentials are available.
- `docs/release-privacy-sandbox.md` is the current privacy/sandbox release note, `docs/release-privacy-policy-draft.md` is the owner-review privacy policy draft, `docs/release-app-store-privacy-answers.md` is the App Store privacy answer draft, and `INSIGHTKIT_PRIVACY_POLICY_URL` is the explicit App Store preflight input for the required public privacy policy URL.

## Remaining Work

- Keep visual GUI proof on bounded Computer Use while XCUITest remains blocked by macOS LocalAuthentication in this environment. Repeatable installed-app import smoke is now covered by `scripts/run_packaged_app_url_import_smoke.py`, but it is URL/app-side automation rather than a screenshot-level UI runner.
- Decide release channel:
  - Direct distribution: paid/active Apple Developer Program, Developer ID certificate, hardened runtime/notarization, stapling, notarized/stapled public archive.
  - Notary credentials: configure a `notarytool` keychain profile such as `insightkit-notary`, or provide Apple ID, Team ID, and app-specific password through the script's arguments/environment variables.
  - Mac App Store: enable sandbox, review file bookmarks, sidecar/subprocess strategy, entitlements, App Review metadata.
- Continue release-hardening PDF rendering quality: RPC `document.export` no longer fails when WeasyPrint is absent, but the built-in fallback is intentionally a simple text PDF rather than full HTML-quality layout.
- Keep future provider checks app-side rather than relying on terminal Keychain reads. GUI-saved DeepSeek credentials and online DeepSeek final-build are now verified through `logs/diagnostics/2026-05-26/app-side-provider-validation-proof.json`; the older CLI Keychain limitation (`security` returncode `44`) remains only a diagnostic limitation of terminal access, not a product capability gap.
- Publish a privacy policy URL and enter final App Store Connect privacy answers; repo drafts now exist, but the public URL and App Store Connect submission remain owner tasks.
- Keep `docs/plans/2026-05-26-insightkit-release-readiness-status.md` current after every release-channel or packaging change so final completion can be audited requirement-by-requirement.
