interface:
  display_name: "AutoTranscribe"
  short_description: "Repo-specific patterns and workflows for AutoTranscribe"
  default_prompt: "Use the AutoTranscribe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true